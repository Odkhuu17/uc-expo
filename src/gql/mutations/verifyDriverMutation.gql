mutation VerifyDriver(
  $passport: Upload!
  $passportBack: Upload!
  $selfie: Upload!
) {
  verifyDriver(
    input: { passport: $passport, passportBack: $passportBack, selfie: $selfie }
  ) {
    id
    status
    field5
  }
}
