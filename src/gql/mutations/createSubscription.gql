mutation CreateSubscription(
  $truckId: ID!
  $subscriptionPlanId: ID!
  $userId: ID!
) {
  createSubscription(
    input: {
      truckId: $truckId
      subscriptionPlanId: $subscriptionPlanId
      userId: $userId
    }
  ) {
    id
    payments {
      id
    }
  }
}
